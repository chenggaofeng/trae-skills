interface:
  display_name: "OpenClaw Local Rescue"
  short_description: "Diagnose and rescue a local OpenClaw from tx-2 guidance, including when the user says 救援mac"
  default_prompt: "When the user says 救援mac or asks to rescue the local OpenClaw, use $openclaw-local-rescue to diagnose why the local OpenClaw stopped responding and keep bailian/kimi-k2.5 as the default model."
policy:
  allow_implicit_invocation: true
